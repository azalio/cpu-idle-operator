apiVersion: v2
name: cpi-idle-operator
description: >-
  Node agent DaemonSet giving pods opt-in access to two cgroup v2 CPU tiers
  (cpu.idle, cpu.max.burst) through pod annotations. Source of truth for
  config/base -- see the "manifests" Makefile target.
type: application
version: 0.1.0
appVersion: "latest"
home: https://github.com/azalio/cpi-idle-operator
