# Code review 001 — ST-014 (hack/stand-probe.sh)

Итерации Monitor: 3. Итог: valid = true.

## Итерация 1 — valid: false, четыре находки

| Тяжесть | Находка | Что сделано |
|---|---|---|
| HIGH | Текст `--help` обещал, что режим `--pod-uid` «только измеряет», хотя скрипт пишет `cpu.idle`, `cpu.weight` и `cpu.max.burst` в cgroup чужого пода и не может проверить принадлежность UID | Секция «Режимы» переписана с явным предупреждением; в `run_checks` добавлены два лог-предупреждения с конкретным путём cgroup. Интерактивного подтверждения не добавляла: в этом окружении действует правило не требовать его в скриптах |
| MEDIUM | Строка таблицы «соседний под форсирует перезапись слайса» сравнивала литерал сама с собой, а `create_neighbour` глушила ошибку через `\|\| true` — строка структурно не могла провалиться | `\|\| true` убран, функция возвращает код kubectl, вызывающий код кладёт его в `neighbour_rc` и передаёт в `record` фактическое значение |
| MEDIUM | Знаменатель формулы веса `262140` вместо kubelet'овского `262142` | Исправлено в коде и комментарии. На 500m оба варианта дают 20 из-за целочисленного деления, поэтому дефект был бы незаметен. Пересчёт: 0m→1, 100m→4, 500m→20, 1000m→39, 2000m→79, 4000m→157 |
| LOW | `kubectl create namespace` не идемпотентен: повторный запуск застаёт namespace в Terminating и падает с кодом 1, что по семантике скрипта означает «расхождение эталона» | Обёрнут в `die` с кодом 2 и объяснением |

## Итерация 2 — valid: false, одна находка

Правка HIGH оказалась неполной: `usage()` исправлен, а идентичная ложная фраза осталась в заголовочном докблоке файла. Регресс той же природы, что и исходная находка. Шапка переписана, формулировки трёх мест сверены между собой.

## Итерация 3 — valid: true

Все четыре находки закрыты, новых дефектов нет, противоречий между шапкой, `usage()` и предупреждениями в `run_checks` не осталось.

## Проверки

- `shellcheck` — чисто (VC3)
- `bash -n` — чисто
- `--dry-run` — код 0, ничего не создаёт
- Полный прогон на стенде (ядро 6.17, k8s 1.36.3, containerd 2.2.1, systemd, cgroup v2): девять строк OK, одна ПРОПУСК, код 0
- Негативный прогон (заранее выставлен `cpu.idle=1`, базовая строка обязана разойтись): строка 1 ПРОВАЛ, код 1 — это и есть ручная проверка VC1
- Отказы окружения: не-v2 cgroup → код 2, несуществующий UID пода → код 2, `--settle` меньше 65 → код 2

## Что осталось непокрытым и почему

- Строка «соседний под форсирует перезапись слайса» на стенде вышла ПРОПУСК: на worker есть бинарь kubectl, но не настроен kubeconfig, а поды создавались с control plane. Эффект соседнего пода на per-pod значение измерен вручную ранее (вес Burstable-слайса менялся 262 → 270, `cpu.idle` не дрогнул), но именно этой строкой автоматически не подтверждён.
- Ветка cgroupfs-драйвера: ноды с ним нет. Скрипт отказывается работать с явным сообщением, что ветка не покрыта, и не выдаёт это за пройденную проверку.
