{
  "status": "OK",
  "confidence": 0.9,
  "search_stats": {
    "files_scanned": 8,
    "total_matches_found": 9,
    "results_truncated": false
  },
  "relevant_locations": [
    {
      "path": ".map/wayfind/cpu-idle-operator/resolutions/T-007.md",
      "lines": [
        1,
        43
      ],
      "relevance": "Решение: агент при остановке ничего не откатывает. Откат на SIGTERM превратил бы rolling update DaemonSet в инцидент по p99 прода и всё равно не сработал бы при SIGKILL, OOM или падении ноды. Осознанный откат вынесен в отдельный режим --revert-all (ST-011). При старте — идемпотентная реконсиляция всей ноды."
    },
    {
      "path": ".map/wayfind/cpu-idle-operator/resolutions/T-009.md",
      "lines": [
        1,
        50
      ],
      "relevance": "Поведение на неподдерживаемой ноде: не крешлупить. Одна ошибка в лог, gauge с лейблом причины, readiness не готов, событие на каждый аннотированный под. Мотив: смешанный кластер — нормальная реальность, а DaemonSet, циклически перезапускающийся на трети нод, топит алерты в шуме."
    },
    {
      "path": "internal/agent/reconciler.go",
      "lines": [
        1,
        150
      ],
      "relevance": "Готовый Reconciler из ST-009 и его конструктор — main обязан собрать именно его, а не свою копию цикла."
    },
    {
      "path": "internal/envgate/gate.go",
      "lines": [
        1,
        150
      ],
      "relevance": "Check возвращает решение и ничего не пишет. Решение о поведении принимает main."
    }
  ],
  "findings": {
    "no_revert_on_shutdown": "INV-4, ключевое: откат на SIGTERM превратил бы обычный rolling update оператора в инцидент по p99 прода — измерено, что фон без idle ломает SLO. И он всё равно недостижим при SIGKILL/OOM, то есть корректность держалась бы только в половине сценариев.",
    "no_crashloop": "На непройденном гейте процесс живёт, readiness 503 с причиной, gauge выставлена, informer в режиме только-чтение вешает Event на аннотированные поды, applier НЕ создаётся вовсе (INV-5).",
    "metrics_double_registration": "Предупреждение от автора ST-009: agent.NewReconciler принимает *observe.Metrics, а apply.NewApplier строит свой Metrics на переданном реестре. При наивной сборке в main это даст двойную регистрацию одинаковых имён на одном prometheus.Registry и панику. Собрать надо один экземпляр Metrics и передать обоим, либо развести реестры.",
    "readiness_two_conditions": "200 только после успешного гейта И первичной синхронизации кеша informer'а. Готовность до синка означала бы, что под объявлен готовым, ничего ещё не применив.",
    "startup_reconcile": "При старте агент идемпотентно приводит ноду в соответствие, не полагаясь на память о прошлой жизни: один код-путь для первого запуска, рестарта, апгрейда и восстановления после падения."
  }
}
