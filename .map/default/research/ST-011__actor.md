{
  "status": "OK",
  "confidence": 0.9,
  "search_stats": {
    "files_scanned": 5,
    "total_matches_found": 6,
    "results_truncated": false
  },
  "relevant_locations": [
    {
      "path": ".map/wayfind/cpu-idle-operator/resolutions/T-007.md",
      "lines": [
        1,
        43
      ],
      "relevance": "Обоснование существования режима: агент при остановке ничего не откатывает, поэтому при удалении оператора из кластера idle-поды остались бы idle навсегда. Откат вынесен в явную операцию, которую человек РЕШИЛ выполнить, а не в побочный эффект рестарта процесса."
    },
    {
      "path": "internal/apply/revert.go",
      "lines": [
        1,
        108
      ],
      "relevance": "Готовый Applier.Revert из ST-008: порядок cpu.idle=0, вес из текущего spec, снятие burst. ErrCgroupGone уже трактуется как тихий возврат."
    },
    {
      "path": "internal/agent/lifecycle.go",
      "lines": [
        1,
        150
      ],
      "relevance": "Обычный путь запуска — informer, метрики, health. Режим --revert-all не должен поднимать ничего из этого."
    }
  ],
  "findings": {
    "why_exists": "Агент не откатывает на выходе принципиально. Значит при удалении оператора idle-поды останутся idle навсегда и снять это будет нечем — ручной обход всех нод. Режим закрывает именно это.",
    "explicit_not_hook": "Разница принципиальна: откат становится тем, что человек решил сделать, а не побочным эффектом рестарта процесса.",
    "no_stop_on_first_error": "Один сбойный под не должен оставить остальные необработанными: пройти всех, потом вернуть ненулевой код.",
    "cgroup_gone_is_not_error": "Под мог исчезнуть между List и записью. ErrCgroupGone — штатная гонка, не ошибка отката.",
    "reuse_revert": "Звать готовый Applier.Revert, не дублировать порядок записей: он физически обязателен, ядро отвергает запись веса при idle=1."
  }
}
