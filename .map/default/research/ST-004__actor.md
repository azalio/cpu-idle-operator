{
  "status": "OK",
  "confidence": 0.95,
  "search_stats": {
    "files_scanned": 5,
    "total_matches_found": 6,
    "results_truncated": false
  },
  "relevant_locations": [
    {
      "path": ".map/wayfind/cpu-idle-operator/resolutions/T-006.md",
      "lines": [
        1,
        60
      ],
      "relevance": "Формула веса и её обоснование: kubelet выводит cpu.weight из requests.cpu, ядро при снятии idle оставляет 100 вместо request-derived, поэтому агент обязан пересчитывать сам. Внимание: в тексте резолюции знаменатель записан как 262142-2 — это ошибка транскрипции, пойманная позже; верный знаменатель 262142."
    },
    {
      "path": ".map/wayfind/cpu-idle-operator/resolutions/T-005.md",
      "lines": [
        1,
        108
      ],
      "relevance": "Измеренная на стенде пара: requests.cpu 500m даёт cpu.weight 20 у реального пода, выставленного kubelet. Это эталон для VC2."
    },
    {
      "path": "hack/stand-probe.sh",
      "lines": [
        95,
        244
      ],
      "relevance": "Функция expected_weight — та же формула на shell, уже с исправленным знаменателем 262142 и комментарием о том, почему ошибка в 262140 не проявлялась бы на 500m."
    },
    {
      "path": ".map/default/spec_default.md",
      "lines": [
        1,
        150
      ],
      "relevance": "Решение 14: QoS-класс вычисляем сами чистой функцией от spec, status.qosClass только сверка. Причина — поле статуса на свежесозданном поде может отсутствовать, а от класса зависит путь pod-cgroup."
    }
  ],
  "findings": {
    "formula": "shares = max(2, milliCPU*1024/1000); weight = 1 + ((shares-2)*9999)/262142, целочисленно",
    "denominator_trap": "Знаменатель ровно 262142. Ошибочный 262140 даёт тот же ответ на 500m из-за целочисленного деления, поэтому дефект не проявился бы на эталонной паре — именно так он и проехал в первой версии стендового скрипта и был пойман только на ревью.",
    "checked_values": "Сверено на исправленной shell-реализации: 0m->1, 1m->1, 100m->4, 500m->20, 1000m->39, 2000m->79, 4000m->157. Значение 1000m->39 — общеизвестная контрольная точка kubelet.",
    "qos_from_spec_not_status": "ClassOf обязан считать из spec. status.qosClass заполняется не нами и на свежесозданном поде может ещё отсутствовать — это гонка на самом горячем пути, а от класса зависит путь pod-cgroup.",
    "pure_functions": "Никакого кеша и состояния. Решение T-006: источник истины при восстановлении веса — актуальный spec, а не запомненное при входе в idle значение, иначе изменившийся во время idle request восстановится неверно."
  }
}
