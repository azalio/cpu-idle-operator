{
  "status": "OK",
  "confidence": 0.85,
  "search_stats": {
    "files_scanned": 6,
    "total_matches_found": 7,
    "results_truncated": false
  },
  "relevant_locations": [
    {
      "path": ".map/wayfind/cpu-idle-operator/resolutions/T-012.md",
      "lines": [
        1,
        56
      ],
      "relevance": "Уровни проверок и прямая фиксация неизвестного: согласованность cgroup-вида внутри kind-ноды НЕ проверена, рекомендация extraMounts взята из непроверенного исследования. Это первый пункт к исполнению, от него зависит существование блокирующего e2e-гейта вообще."
    },
    {
      "path": ".map/default/spec_default.md",
      "lines": [
        1,
        150
      ],
      "relevance": "Open Question 1: если вид не сойдётся — заменить kind одноузловой нодой в CI и зафиксировать причину письменно, а не замалчивать. Подзадача помечена provisional именно поэтому."
    },
    {
      "path": ".map/wayfind/cpu-idle-operator/resolutions/T-005.md",
      "lines": [
        1,
        108
      ],
      "relevance": "Эталонные значения, которые e2e обязан увидеть: cpu.idle=1 после применения, 0 и request-derived вес после снятия. Чтение — из настоящего cgroup-файла, не из логов агента."
    },
    {
      "path": "hack/stand-probe.sh",
      "lines": [
        1,
        150
      ],
      "relevance": "Рабочая проверка тех же фактов на настоящей ноде. e2e в kind не заменяет её: четыре ядерных факта требуют настоящего kubelet."
    }
  ],
  "findings": {
    "preflight_first": "Порядок обязателен: сначала pre-flight, сравнивающий вычисленный агентом путь с реально существующим в ноде kind. Всё остальное строится на его ответе.",
    "fallback_is_legitimate": "Если вид не сойдётся — это не провал подзадачи. Решение зафиксировано заранее: заменить kind одноузловой нодой в CI и записать причину в README. Замалчивать нельзя.",
    "read_from_cgroup_not_logs": "e2e обязан читать фактическое значение из cgroup-файла внутри ноды через exec, а не доверять логам агента. Лог говорит, что агент думает, что сделал.",
    "extramounts_unverified": "Рекомендация extraMounts для /sys/fs/cgroup пришла из исследования, которое в этом проекте уже дважды ошибалось. Применять только если pre-flight покажет, что нужно.",
    "runner_cgroup_v2": "CI-шаг обязан явно проверять, что раннер на cgroup v2, а не предполагать это.",
    "kind_available": "Локально установлен kind v0.29.0 (darwin/arm64); контейнерный движок — podman с работающей машиной, docker-контекст удалённый и bind-mount не поддерживает."
  }
}
